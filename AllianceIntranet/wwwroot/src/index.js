import React from "react";
import ReactDOM from "react-dom";

const Index = () => {
    return <div>react is running in my app.. without node this is real?</div>;
};

ReactDOM.render(<Index />, document.getElementById("root"));